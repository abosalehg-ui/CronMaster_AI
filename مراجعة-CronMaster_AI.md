# 🧪 مِحَك — مراجعة CronMaster_AI

**تاريخ المراجعة:** 2026-08-04
**النوع المكتشف:** تطبيق (أداة سطر أوامر) — لا لعبة. الرابرك المطبَّق: هندسة 45% + UX/UI 30% + أمن 25%
**نطاق المراجعة:** المستودع كاملاً — 26 ملف مصدر (~4,670 سطر) + 12 ملف اختبار (~3,265 سطر). قُرئ كل ملف مصدر بالكامل.
**التحقق العملي:** شُغِّلت الاختبارات (272 نجحت)، و`ruff check` (نظيف)، وقياس تغطية (87%)، وكُتبت اختبارات فحص مؤقتة للتأكد من ثلاث ملاحظات قبل إدراجها.

---

## 1. نظرة عامة سريعة

CronMaster_AI أداة مراقبة وإصلاح ذاتي للمهام المجدولة: تقرأ المهام من واجهة خلفية (OpenClaw CLI افتراضياً أو `crontab` النظام)، تصنّف أسباب الفشل بمطابقة أنماط، تطبّق إصلاحات مسقوفة (رفع المهلة، إعادة محاولة، تعطيل، تراجع، إزاحة جدولة)، ترسل تنبيهات على قنوات متعددة، وتحتفظ بسجل تاريخي في SQLite تُشتق منه إحصائيات وتقرير HTML.

**البنية:** حزمة `cronmaster/` مقسّمة إلى طبقات نظيفة — إعدادات، واجهات خلفية، تحليل، إصلاح، تخزين، تنبيهات، تقارير، مقاييس، ثم منسّق (`core.py`) وواجهة سطر أوامر (`cli.py`). و`CronMaster_AI.py` بقي غلاف توافق رفيع (119 سطر) للمداخل القديمة.

**التقنيات:**
- Python ≥ 3.8 (المُعلن في `pyproject.toml`)، **بلا أي اعتمادية تشغيل** — مكتبة قياسية بحتة.
- اختيارية: `anthropic>=0.40` (مصنّف الأخطاء)، `pytest` + `ruff` (تطوير).
- SQLite للسجل التاريخي، `urllib` للـ webhooks، `fcntl` للقفل، SVG يدوي للرسوم.

**أكبر الملفات:** `core.py` (641)، `storage.py` (621)، `analysis/llm.py` (327)، `reporting/html.py` (321)، `cli.py` (297).

---

## 2. المراجعة التقنية والهندسية (45%)

### 2.1 البنية المعمارية وفصل المسؤوليات — **9/10**

هذي أقوى ناحية في المشروع. الطبقات مفصولة فعلاً لا اسمياً، والاعتماديات تسير في اتجاه واحد (`cli` → `core` → الطبقات، ولا أحد يستورد `cli`).

أفضل قرار معماري: **عقد القدرات (Capability contract)** في `backends/base.py`:

```python
class Capability:
    LIST = "list"
    SET_TIMEOUT = "set_timeout"
    ...
def supports(self, capability: str) -> bool:
    return capability in self.capabilities
```

و`fixers.py` يسأل قبل كل عملية:

```python
if not self.backend.supports(Capability.SET_TIMEOUT):
    analysis.fix_details = t("restore.unsupported", op=Capability.SET_TIMEOUT)
    return analysis
```

النتيجة أن `crontab` — الذي لا يعرف مهلة لكل مهمة ولا حالة آخر تشغيل — يُنتج رسالة "غير مدعوم" واضحة بدل `NotImplementedError` أو سلوك مخترع. هذا نمط صحيح ونادر في مشاريع بهذا الحجم.

الحقن (injection) مطبَّق في كل مكان: `CronMaster(backend=...)`، `HistoryStore(db_path=..., enabled=...)`، `AlertManager(notifiers=..., state=...)`، `LLMClassifier(client=...)` — ولهذا الاختبارات لا تلمس الشبكة ولا عملية فرعية.

**النقطة الوحيدة المؤاخذة:** `core.CronMaster` صار منسّقاً ضخماً — 641 سطر، 20+ دالة، ودالة `monitor()` وحدها ~130 سطر تجمع: الجلب، الترشيح، خمسة مسارات إصلاح، كشف التعافي، اختيار ما يستحق التنبيه، الحفظ، وبناء النتيجة.

**الحل:** استخراج مسارات الإصلاح الخمسة (`_check_circuit_breaker`، `_check_rollback`، `_apply_auto_fix`، `_check_reschedule`، `_check_duration_regression`) إلى وحدة `cronmaster/policies.py` تعرض `apply_policies(job, analysis, ctx) -> PolicyOutcome`، ويبقى `monitor()` حلقة قصيرة تنسّق بينها. لا حاجة لإعادة كتابة — نقل ميكانيكي.

---

### 2.2 قابلية القراءة والصيانة — **9/10**

الدوال قصيرة، والتداخل ضحل، والتسمية مباشرة. لكن الميزة الحقيقية أن التعليقات تشرح **لماذا** لا **ماذا** — وهذا نادر:

`analysis/errors_db.py`:
```python
# الترتيب مهم: الأنماط الأكثر تحديداً أولاً، ونمط TIMEOUT العام أخيراً حتى لا
# تُصنَّف أخطاء مثل "Connection timed out" كمهلة قابلة للإصلاح فيُرفع
# timeout المهمة بلا جدوى.
```

`core.py` — في `_fix_timeout_flow()`:
```python
# أول رفعة فقط تُسجّل نقطة التراجع: نعود إلى آخر مهلة معروفة الصلاح،
# لا إلى الرفعة السابقة التي لم تُجدِ هي الأخرى
```

هذي تعليقات تحفظ قراراً هندسياً كان يمكن أن يُكسر بحسن نية بعد ستة أشهر.

**ملاحظة صغيرة — تسجيل غير موحّد:** الملف الواحد يخلط الأسلوبين:

`core.py` — في `_fix_timeout_flow()`:
```python
self.logger.info(f"🔄 أعيد تشغيل: {job.name}")     # f-string: يُبنى دائماً
```
`core.py` — في `_check_reschedule()`:
```python
self.logger.info("جدولة %s غير قابلة للإزاحة تلقائياً (%s)", job.name, job.schedule)  # كسول
```

**الحل:** توحيد على النمط الكسول (`%s`) في كل الحزمة، وتفعيل قاعدة `G` في `[tool.ruff.lint] select` لتفرضه آلياً.

---

### 2.3 التكرار (DRY) و Code Smells — **8/10**

**(أ) دوال تنسيق مكررة حرفياً بين وحدتين.**

`cli.py`:
```python
def _fmt_pct(value):
    return "—" if value is None else f"{value * 100:.0f}%"

def _fmt_secs(value):
    return "—" if value is None else f"{value:.1f}s"
```

`reporting/html.py`:
```python
def _pct(value: Optional[float]) -> str:
    return "—" if value is None else f"{value * 100:.0f}%"

def _secs(value: Optional[float]) -> str:
    return "—" if value is None else f"{value:.1f}s"
```

نفس المنطق ونفس المخرجات، في ملفين. تغيير صيغة العرض يتطلب تعديلين ونسيان أحدهما لا يكشفه أي اختبار.

**الحل:** وحدة `cronmaster/format.py` فيها `pct()` و`secs()` و`duration()` و`trend()`، ويستوردها الاثنان.

**(ب) `Config` حالة عامة قابلة للتعديل (global mutable state).**

`config.py` يعتمد على `setattr(cls, attr, value)` على صنف بأكمله. النتيجة أن كل شيء في النظام يقرأ من متغير عام، ولهذا `conftest.py` مضطر لسرد **19 خاصية** يدوياً لعزل كل اختبار:

```python
for attr in ("NOTIFIERS", "QUIET_HOURS", "TELEGRAM_CHAT_ID", ... "ALERT_THRESHOLD"):
    monkeypatch.setattr(Config, attr, getattr(Config, attr))
```

إضافة أي إعداد جديد تتطلب تذكّر تحديث هذي القائمة، وإلا تسرّبت الحالة بين الاختبارات بصمت.

**الحل:** تحويل `Config` إلى `@dataclass` غير قابل للتعديل يُبنى مرة في `main()` ويُمرَّر إلى `CronMaster(config=...)`. عزل الاختبار يصير `Config(**overrides)` بدل 19 `monkeypatch`. هذا تغيير واسع لكنه ميكانيكي.

---

### 2.4 معالجة الأخطاء والحالات الحدّية — **6/10**

المبدأ العام ممتاز — **التدهور اللطيف (graceful degradation)** مطبَّق بانضباط: قاعدة تالفة تُعطِّل السجل لهذه الدورة ولا تُسقط `monitor` (`storage.HistoryStore._disable`)، وقفل غير قابل للفتح يُكمل بلا قفل (`lock.acquire`)، وقناة تنبيه واحدة لا تُسقط البقية (`notifiers.AlertManager.dispatch`)، وغياب حزمة `anthropic` سطر INFO واحد. وفشل الواجهة الخلفية **يُبلَّغ صراحة** ولا يُقدَّم كنجاح — وهذا القرار موثّق في README ومُختبَر.

لكن فيه ثلاث فجوات حقيقية، أولاها الأخطر في التقرير كله.

---

#### 🔴 (أ) تصعيد "الإصلاح غير المجدي" يتجاوز تهدئة التنبيهات كلياً — تنبيه في **كل** دورة مراقبة

`core.py` — في `_fix_timeout_flow()`:

```python
applied_before = self.state.get_timeout_fixes(job.id)
if applied_before >= Config.MAX_TIMEOUT_FIXES:
    analysis.fix_details = t("alert.futile_fix", name=job.name, count=applied_before)
    self._escalate(analysis.fix_details)      # ← بلا أي فحص تهدئة
    return 0, 0
```

و`_escalate` يضيف السطر إلى `self._extras`، وشرط الإرسال في `monitor()`:

```python
if alert and (alert_analyses or recovered or alert_silent or self._extras or self._critical):
```

`self._extras` وحده يكفي للإرسال. لا يمرّ على `should_alert()` ولا يُسجَّل بـ `record_alert()`.

**قارن بالمسارات الأخرى** — كلها محروسة:
- `_check_duration_regression`: `if not dry_run and not self.state.should_alert(job.id, "duration_regression"): return`
- `_check_circuit_breaker`: `if self.state.is_circuit_open(job.id): return False`
- `_check_reschedule`: `if ... or self.state.has_reschedule(job.id): return False`

مسار الإصلاح غير المجدي هو الوحيد بلا حارس.

**التحقق العملي** — شغّلت خمس دورات `monitor()` متتالية على مهمة استنفدت رفعاتها:

```
>>> عدد التنبيهات المرسلة في 5 دورات متتالية: 5
--- تنبيه 2 --- (مطابق حرفياً لـ 3 و4 و5)
🚨 تنبيه CronMaster
🛑 رفع المهلة لم يعد مجدياً لـ Failing Job: طُبّق 3 مرة والمهمة ما تزال تفشل...
```

**لماذا هذا خطير:** README يَعِد صراحةً — *«لا يتكرر نفس التنبيه (نفس المهمة ونفس نوع الخطأ) قبل `alert_cooldown_hours`»* (افتراضياً 24 ساعة). المستخدم الذي يجدول `monitor` كل خمس دقائق يستلم **288 رسالة Telegram متطابقة يومياً** عن مهمة واحدة. والنتيجة العملية أسوأ من الإزعاج: التنبيهات تفقد معناها، والمستخدم يكتم القناة، فيضيع التنبيه المهم القادم. والمفارقة أن الرسالة نفسها تقول «المطلوب تدخل بشري» — فتقتل احتمال التدخل البشري بإغراقه.

**الحل** (تعديل من ثلاثة أسطر في `core.py`):

```python
if applied_before >= Config.MAX_TIMEOUT_FIXES:
    analysis.fix_details = t("alert.futile_fix", name=job.name, count=applied_before)
    if dry_run or self.state.should_alert(job.id, "futile_fix"):
        self._escalate(analysis.fix_details)
        if not dry_run:
            self.state.record_alert(job.id, "futile_fix")
    return 0, 0
```

وبما أن `clear_alerts_for(job_id)` تُستدعى عند التعافي، سيعود التنبيه فوراً إن عادت المشكلة. **ويُضاف اختبار** يثبت أن دورتين متتاليتين تنتجان تنبيهاً واحداً — لا يوجد اليوم اختبار يغطي التكرار عبر الدورات، ولهذا مرّت الثغرة.

---

#### 🟠 (ب) قيمة نصية خاطئة في `config.json` تُسقط الأداة بـ traceback خام

`config.py` — في `load()`، مفاتيح `_STR_KEYS` تُسنَد بلا أي فحص نوع:

```python
for key, value in data.items():
    attr = key.upper()
    if attr in configurable:
        setattr(cls, attr, value)     # ← لا isinstance
```

و`validate()` تحرس الأرقام بدقة (`_clamp` على 15 مفتاحاً) وتحرس `NOTIFIERS` و`QUIET_HOURS` — لكنها **لا تفحص ولا مفتاحاً نصياً واحداً**: `BACKEND`, `LANG`, `LLM_MODEL`, `TELEGRAM_CHAT_ID`, `HEALTHCHECK_PING_URL`, `PROMETHEUS_TEXTFILE`.

**التحقق العملي** بملف إعدادات فيه `{"lang": 5, "backend": 7, "telegram_chat_id": 123456}`:

```
>>> بعد load(): LANG=5 BACKEND=7 CHAT=123456
>>> set_lang:    AttributeError: 'int' object has no attribute 'strip'
>>> get_backend: AttributeError: 'int' object has no attribute 'strip'
```

`cli.main()` يلتقط `BackendError` فقط، فالمستخدم يرى traceback بايثوني خاماً — في أداة كامل فلسفتها «صحّح القيم المستحيلة بهدوء بدل السقوط لاحقاً» (تعليق `validate()` نفسه).

وحالة `telegram_chat_id` أخبث، لأنها **الخطأ الأكثر احتمالاً**: معرّف Telegram رقم، فمن الطبيعي جداً كتابته `123456` بلا علامتي تنصيص. النتيجة أن `run_openclaw(..., "--to", 123456)` يرفع `TypeError: expected str, bytes or os.PathLike object, not int` — يبتلعه `dispatch()` في `except Exception` ويسجّله. أي أن **التنبيهات تتوقف بصمت** والمستخدم لا يعرف إلا بقراءة السجل.

**الحل** — في `config.validate()`:

```python
for attr in cls._STR_KEYS:
    value = getattr(cls, attr)
    if not isinstance(value, str):
        logger.warning("قيمة %s يجب أن تكون نصاً (%r) — حُوّلت", attr, value)
        setattr(cls, attr, "" if value is None else str(value))
```

`str(123456)` يعطي `"123456"` وهو المطلوب تماماً، و`str(5)` يعطي `"5"` فتلتقطه `set_lang` وتحذّر وتُبقي العربية. ويُضاف فحص `doctor` يعرض أنواعاً غير متوقعة.

---

#### 🟡 (ج) `main()` يلتقط `BackendError` وحده

`cli.py`:
```python
except BackendError as e:
    print(t("cli.backend_error", error=e), file=sys.stderr)
    sys.exit(EXIT_MONITOR_FAILURE)
```

أي `OSError` (مجلد التقارير للقراءة فقط، القرص ممتلئ أثناء كتابة تقرير) أو `sqlite3.Error` غير متوقع يخرج كـ traceback.

**الحل:** إضافة `except Exception as e:` أخير يسجّل الاستثناء كاملاً في ملف السجل، ويطبع للمستخدم سطراً عربياً واحداً يشير إلى `~/.cronmaster/cronmaster.log`، ويخرج بـ `EXIT_MONITOR_FAILURE`.

---

### 2.5 الأداء واستهلاك الموارد — **6/10**

أضعف ناحية بعد معالجة الأخطاء. ثلاث مشاكل، كلها في مسار يعمل كل دورة.

#### 🟠 (أ) N+1 في `job_summary` — 101 استعلام لعشرين مهمة

`storage.py` — `job_summary()` تستدعي خمس دوال، **كلٌّ منها يفتح استعلاماً مستقلاً على نفس الجدول**:

```python
trend = self.duration_trend(job_id)                    # → durations() → _rows()
rows = self._rows(job_id, since)                       # استعلام 2
"success_rate": self.success_rate(job_id, since),      # → _rows()
"flakiness":    self.flakiness_score(job_id),          # → _rows()
"mtbf_seconds": self.mean_time_between_failures(job_id),  # → _rows() بلا LIMIT
```

و`core.stats()` و`core.report(fmt="html")` تستدعيانها **لكل مهمة**.

**القياس العملي** (20 مهمة × 10 دورات مسجَّلة، بتتبع `set_trace_callback`):

```
>>> job_summary لـ 20 مهمة = 101 استعلام SELECT
```

ولاحظ أن `mean_time_between_failures` تسحب **كل** صفوف المهمة بلا `LIMIT` ولا نافذة زمنية، ثم تحوّل كل طابع زمني في بايثون بـ `datetime.fromisoformat` وتفرزه. مع الاحتفاظ الافتراضي 90 يوماً ودورة كل 5 دقائق، هذي ~25,900 صف للمهمة الواحدة تُقرأ وتُحلَّل في الذاكرة لحساب رقم واحد.

**الحل:** استعلام تجميعي واحد لكل مهمة يحسب `COUNT`, `SUM(status='ok')`, `AVG(duration_seconds)` في SQL، ونافذة `LIMIT` على `mtbf`، أو الأفضل استعلام واحد بـ `GROUP BY job_id` يعيد ملخص **كل** المهام دفعة واحدة (الفهرس `idx_runs_job_obs` موجود أصلاً ويخدم هذا).

#### 🟠 (ب) `commit` لكل صف في `record_cycle`

`storage.py` — `record_observation()` تُنهي كل إدراج بـ `self._conn.commit()`، و`record_cycle()` تناديها في حلقة:

```
>>> record_cycle لـ 20 مهمة = 60 تنفيذ، منها 20 COMMIT
```

كل `commit` في SQLite = عملية fsync على القرص. عشرون fsync متسلسلاً كل دورة، والمطلوب واحد.

**الحل:** `executemany` داخل معاملة واحدة، و`commit` واحد في نهاية `record_cycle`.

#### 🟡 (ج) `record_fix` تكتب ملف الحالة كاملاً في كل مرة

`storage.py` — `StateManager.record_fix()` تنتهي بـ `self.save()`، و`save()` تُسلسل الحالة كلها (مع `indent=2`) إلى ملف مؤقت ثم `chmod` ثم `os.replace`. في دورة فيها عشر مهام مُصلَحة = عشر كتابات كاملة للملف، بالإضافة إلى `save()` النهائية في `monitor()`.

**الحل:** إزالة `self.save()` من `record_fix` والاعتماد على الحفظ الواحد في نهاية `monitor()` — الذي يجري فعلاً. المسار الوحيد المتأثر هو `core.fix_job()` وهو ينادي `self.state.save()` صراحةً بعده أصلاً.

**ملاحظة إنصاف:** لا شيء من هذا يظهر على 5–10 مهام، وهو الاستخدام المتوقع. الملاحظة مسجَّلة لأن الأداة تُسوَّق للتشغيل الدوري الطويل، والتكلفة تتراكم مع مدة الاحتفاظ لا مع عدد المهام.

---

### 2.6 الاختبارات — **9/10**

272 اختباراً، كلها تنجح، تغطية **87%** على `cronmaster/` (2,298 عبارة، 299 غير مغطاة). ونسبة الاختبار إلى المصدر 3,265 : 4,672 — نسبة صحية.

الأقوى أن العزل **حقيقي** لا ادعائي: لا شبكة، لا عملية فرعية، لا مجلد مستخدم. `conftest.py` يوفّر `FakeBackend` يسجّل كل عملية (وفيه `mutations()` المخصصة لإثبات أن `--dry-run` لم يعدّل شيئاً — فكرة ممتازة)، ووحدة `anthropic` مزيفة تحمل نفس أسماء الاستثناءات، وصنف `Anthropic` فيها يرفع `AssertionError("يجب ألا يُبنى عميل حقيقي داخل الاختبارات")` — حارس صريح ضد تسرّب نداء فعلي.

**الفجوات المحددة:**

| الوحدة | التغطية | ما ينقص |
|---|---|---|
| `notifiers/telegram.py` | 66% | مسارات فشل الإرسال وغياب `chat_id` |
| `fixers.py` | 77% | مسارات `restore` الجزئي وفشل النسخ الاحتياطي |
| `analysis/analyzer.py` | 80% | تكامل المصنّف الذكي مع `use_llm=False` |

**والفجوة الأهم ليست في التغطية بل في نوع الاختبار:** كل الاختبارات تفحص **دورة واحدة**. لا يوجد اختبار يشغّل `monitor()` مرتين متتاليتين ويفحص السلوك عبر الدورات — ولهذا تحديداً مرّت ثغرة تكرار التنبيه (2.4-أ). الاختبارات لم تفشل لأن أحداً لم يسأل السؤال.

**الحل:** ملف `tests/test_multi_cycle.py` يشغّل `monitor()` 3–5 مرات ويثبت خصائص عبر الزمن: التنبيه لا يتكرر، عدّادات المحاولة لا تتجاوز السقف، `state.json` لا ينمو بلا حد، ولا يتكرر صف في `runs` لنفس `run_key`.

---

### 2.7 التوثيق — **9/10**

README من أفضل ما يُكتب لمشروع بهذا الحجم: 428 سطراً فيها أمثلة تشغيل حقيقية مع مخرجاتها، جدول كامل لأكواد الخروج ومعانيها، جدول بكل مفتاح إعدادات مع متغير البيئة المقابل والافتراضي، شجرة ملفات مع الأذونات، وقسم يشرح **لماذا** لا تُصلَح بعض الأخطاء تلقائياً («الأداة تُصلح إعدادات المهمة لا كود المستخدم ولا بيئة النظام»). حتى الفخ التشغيلي موثّق: تحذير أن `monitor` يعيد `2` و`3` في حالات ليست أخطاءً.

**ملاحظتان:**

**(أ) `PROJECT_REVIEW.md` صار مضلِّلاً.** الملف مراجعة قديمة (2026-07-18) لبنية ما قبل الحزمة، ونطاقها المعلن يذكر ملفات **لم تعد موجودة**:

```
**نطاق المراجعة:** كامل المستودع (`CronMaster_AI.py`، `run_status.py`، `run_test.py`، ...)
```

`run_status.py` و`run_test.py` غير موجودَين، والملف يشير إلى أرقام أسطر في ملف واحد ضخم تفكّك منذ ذلك الحين إلى 26 وحدة. قارئ جديد يفتحه ظانّاً أنه وصف للوضع الحالي.

**الحل:** نقله إلى `docs/archive/PROJECT_REVIEW-2026-07.md` مع سطر في رأسه: «مراجعة تاريخية لبنية ما قبل الإصدار 3 — محفوظة للسياق، لا تصف الكود الحالي».

**(ب) وعد Python 3.8 غير مُختبَر.** `pyproject.toml` يعلن `requires-python = ">=3.8"` و`ruff` مضبوط على `target-version = "py38"`، لكن مصفوفة CI تختبر **3.9 و3.12 فقط**. و`notifiers/__init__.py` يحمل مساراً خاصاً لغياب `zoneinfo` (المضافة في 3.9):

```python
except ImportError:
    logging...warning("zoneinfo غير متاحة على هذا الإصدار من Python — ستُحسب فترة الهدوء بالتوقيت المحلي")
```

أي أن 3.8 مدعوم نظرياً بميزة ناقصة، وغير محروس بأي اختبار.

**الحل:** إما إضافة `"3.8"` إلى مصفوفة CI، أو رفع `requires-python` إلى `">=3.9"` وحذف مسار السقوط الميت. الثاني أنظف.

---

### 2.8 اتفاقيات التسمية ونمط موحّد — **9/10**

`snake_case` للدوال والمتغيرات، `PascalCase` للأصناف، `UPPER_CASE` للثوابت — بلا استثناء واحد في 26 ملفاً. `ruff` نظيف بقواعد `E, F, W, B` وسطر 120. البادئة `_` مستعملة باتساق للخاص. و`__all__` مصرَّح في كل `__init__.py` ومرتَّب أبجدياً.

نقطة أنيقة: الأسماء المهجورة موثّقة بوضوح بدل حذفها بصمت —

`backends/openclaw.py`:
```python
# اسم مهجور محفوظ للتوافق مع الكود القديم
OpenClawCronParser = OpenClawBackend
```

وأذكى منها `_CompatModule` في `CronMaster_AI.py` التي تمرّر أي إسناد إلى موطن الاسم الأصلي، فيبقى ترقيع الكود القديم عاملاً بعد التفكيك.

**المؤاخذة الوحيدة:** خلط أسلوبَي التسجيل المذكور في 2.2.

---

### 📊 متوسط القسم الهندسي

| النقطة | الدرجة |
|---|:---:|
| البنية المعمارية وفصل المسؤوليات | 9 |
| قابلية القراءة والصيانة | 9 |
| التكرار وCode Smells | 8 |
| معالجة الأخطاء والحالات الحدّية | 6 |
| الأداء واستهلاك الموارد | 6 |
| الاختبارات | 9 |
| التوثيق | 9 |
| اتفاقيات التسمية | 9 |

**متوسط القسم: 8.2/10**

---

## 3. المظهر والتصميم وتجربة المستخدم (30%)

**⚠️ تعديل المعيار:** المشروع أداة سطر أوامر بلا واجهة تفاعلية، فمعايير «الاستجابة» و«عدد النقرات» لا تنطبق. عُدِّل المعيار إلى: وضوح مخرجات الطرفية، جودة رسائل الخطأ، جودة `--help`، والتغذية الراجعة. **لكن** المشروع **ينتج فعلاً واجهة رسومية** — تقرير HTML كامل — فقُيّمت بمعايير الويب الحقيقية (RTL، إمكانية وصول، وضع داكن، استجابة).

### 3.1 وضوح مخرجات الطرفية — **7/10**

الأوامر المخصصة للقراءة البشرية مُنسَّقة بعناية — `status` و`stats` و`doctor` كلها بجداول ورموز ومحاذاة، و`doctor` يميّز بصرياً بين ثلاث حالات (`✅` سليم / `❌` فشل جوهري / `⚠️` تحذير غير جوهري) وهو تمييز مفيد فعلاً.

**لكن أربعة أوامر من تسعة تسكب JSON خاماً على الطرفية:** `monitor`، `list`، `fix`، `history`:

`cli.py`:
```python
print(json.dumps(result, indent=2, ensure_ascii=False))
```

مخرجات `monitor` تحديداً كائن فيه `analyses` بكل تفاصيل كل مهمة فاشلة — عشرات الأسطر. المستخدم الذي يشغّل `monitor` يدوياً ليرى ما حدث يواجه جداراً من JSON بدل ثلاثة أسطر ملخّص، بينما الأداة تملك أصلاً كل الترجمات اللازمة في `i18n.py`.

**الحل:** إضافة `--json` لهذي الأوامر الأربعة، وجعل الافتراضي عرضاً بشرياً (سطر ملخص + جدول بالمهام الفاشلة فقط)، مع إبقاء JSON هو الافتراضي حين لا يكون `stdout` طرفية (`sys.stdout.isatty()`) حتى لا تنكسر السكربتات القائمة.

### 3.2 جودة رسائل الخطأ — **8/10**

الرسائل عربية ومحددة وقابلة للتنفيذ، لا مجرد إعلان فشل:

```
"أمر openclaw غير موجود في PATH — هل هو مثبت؟"
"TELEGRAM_CHAT_ID غير مضبوط — لن يُرسل التنبيه. اضبطه في config.json أو متغير البيئة CRONMASTER_TELEGRAM_CHAT_ID"
"حزمة anthropic غير مثبتة — سيُكتفى بالتصنيف النمطي. للتفعيل: pip install 'cronmaster-ai[ai]'"
```

كل واحدة تقول: ما المشكلة، وما أثرها، وكيف تُحل. هذا مستوى عالٍ.

**الخصم** للفجوة المذكورة في 2.4-ج: الاستثناءات غير المتوقعة تخرج كـ traceback، وهو تناقض حاد مع بقية التجربة.

### 3.3 جودة `--help` — **9/10**

`build_arg_parser` يستعمل `RawDescriptionHelpFormatter` مع epilog فيه عشرة أمثلة تشغيل حقيقية، وسطر يشرح أكواد خروج `monitor` — وهو بالضبط ما يحتاجه من يجدول الأداة:

```
أكواد خروج monitor: 0 سليم، 1 فشل المراقب، 2 مهام فاشلة، 3 طُبِّقت إصلاحات
```

كل أمر فرعي له `help`، وكل علم كذلك. الأمر بلا وسائط يطبع المساعدة بدل خطأ.

### 3.4 العربية و RTL (تقرير HTML) — **8/10**

`reporting/html.py` صفحة مكتفية بذاتها فعلاً — لا CSS ولا JS ولا خط ولا صورة خارجية، وصفر طلبات شبكة. وهذا قرار صحيح لملف يُفتح غالباً بلا إنترنت.

ما نُفِّذ صح:
- `<html lang="{lang}" dir="{direction}">` مشتق من اللغة الفعّالة لا مثبَّت.
- `text-align: start` بدل `left` — ينعكس تلقائياً مع الاتجاه. هذي علامة على أن الكاتب يعرف RTL فعلاً لا يقلّده.
- الخطوط: `system-ui, -apple-system, "Segoe UI", Tahoma, "Noto Naskh Arabic", sans-serif` — احتياطي عربي صريح، بلا تحميل خط عبر الشبكة (حمولة صفر).
- وضع داكن عبر `@media (prefers-color-scheme: dark)` مع متغيرات CSS مضبوطة للوضعين.
- الأرقام غربية (`87.5%`) باتساق في كل المخرجات — وهو الاختيار الصحيح لتقرير تقني.

**الملاحظة:** جداول التقرير تجمع `white-space: nowrap` مع `min-width: 40rem`:

```css
table { border-collapse: collapse; width: 100%; min-width: 40rem; ... }
th, td { ... white-space: nowrap; }
```

على شاشة جوال (~20rem) هذا يعني تمرير أفقي إجبارياً لكل جدول. الحاوية `.scroll { overflow-x: auto }` تمنع كسر الصفحة (والجسم فيه `overflow-x: hidden`) فالضرر محصور — لكن قراءة جدول المهام على الجوال تبقى مزعجة.

**الحل:** إزالة `nowrap` عن عمودَي «المهمة» و«التفاصيل» (الأطول والأكثر قابلية للالتفاف)، وإبقائها على أعمدة الأرقام والجدولة. أو أفضل: عند `@media (max-width: 40rem)` تحويل الجدول إلى بطاقات (`display: block` لكل صف مع `data-label` قبل كل خلية).

### 3.5 إمكانية الوصول — **8/10**

- كل رسم SVG له `role="img"` و`aria-label` مترجَم:
  `<svg viewBox="..." role="img" aria-label="{_e(t("html.trend"))}" width="100%">`
- الحالة **لا** تُنقل باللون وحده: `_status_cell` يطبع النص (`ok` / `error` / `disabled`) بالإضافة إلى الصنف اللوني — فقارئ الشاشة ومصاب عمى الألوان يصلهما نفس المعلومة.
- التباين مقبول في الوضعين (`#1a1a1c` على `#f7f7f8`، `#e9e9ee` على `#16161a`).
- `<meta name="viewport">` موجود، و`line-height: 1.6` مريح للنص العربي.

**الناقص:** الجداول بلا `<caption>` وبلا `scope="col"` على `<th>` — قارئ الشاشة يعلن الخلايا بلا ربطها برؤوسها.

**الحل:** `<th scope="col">` (تعديل سطر واحد في `_jobs_table` و`_history_table`)، و`<caption class="sr-only">` بنص من `i18n`.

### 3.6 i18n — **6/10** ← أضعف نقطة في هذا القسم

`--lang en` موثّق في README كميزة، لكنه ينتج **مخرجات مختلطة** فعلياً. التحقق العملي:

```
>>> مخرجات status بـ --lang en:
========================================
📊 Scheduled job status
========================================
Total jobs:   8
Healthy ✅:        7
Failing ❌:        1
Critical ⚠️:         0
Silent 😴:        0
Success rate:     87.5%
========================================

>>> تصنيف بالإنجليزية: انتهاء المهلة الزمنية - العملية أخذت وقت أطول من المسموح | زيادة timeout
>>> غير معروف: خطأ غير مصنف | راجع السجلات
```

مشكلتان:

**(أ) المحاذاة مكسورة بالإنجليزية.** `cli._print_status` يستعمل مسافات ثابتة مضبوطة يدوياً — والتعليق يعترف بالسبب:

```python
# المسافات مضبوطة يدوياً لتطابق المخرجات العربية التاريخية حرفياً
print(f"{t('status.total')}   {result['total_jobs']}")
print(f"{t('status.ok')}        {result['ok']}")
```

المسافات مُعايَرة لأطوال النصوص العربية، فالإنجليزية تخرج متعرّجة كما في المخرَج أعلاه.

**الحل:** حشو محسوب بدل ثابت — `f"{label:<20}{value}"` مع اشتقاق العرض من `max(len(l) for l in labels)`. يعطي محاذاة صحيحة في اللغتين ويلغي التعليق الاعتذاري.

**(ب) نصوص عربية مكتوبة داخل الكود لا تمر بـ `i18n` أصلاً:**

| الموضع | النص المتسرِّب |
|---|---|
| `analysis/errors_db.py` | كل `description_ar` و`suggested_fix` — **9 توقيعات** بالعربية فقط |
| `analysis/analyzer.py` — `analyze_regex()` | `"خطأ غير مصنف"` / `"راجع السجلات"` |
| `fixers.py` — `_fix_timeout()` | `f"تم زيادة timeout من {current}s إلى {new}s"` |
| `core.py` — مسارات dry-run | `f"[dry-run] سيُرفع timeout من {current}s إلى {new}s"` |
| `core.py` — `_retry_transient_flow()` | `"استُنفدت محاولات إعادة التشغيل التلقائية..."` |

وهذي ليست نصوصاً هامشية — إنها **جوهر ما يقرأه المستخدم**: سبب فشل مهمته وماذا فُعل حيالها. مستخدم إنجليزي يرى واجهة إنجليزية وتحليلاً عربياً.

**الحل:** نقل `description_ar`/`suggested_fix` من `errors_db.py` إلى مفاتيح i18n (`error.timeout.desc`, `error.timeout.fix`, …) وجعل `ErrorSignature` تحمل `key` بدل نص، وتمرير رسائل الإصلاح في `fixers.py` و`core.py` عبر `t()`. تعديل ميكانيكي واضح النطاق. وحتى يُنجَز، **الأمانة تقتضي** تعديل سطر README من «مخرجات إنجليزية اختيارية» إلى «واجهة الأوامر بالإنجليزية؛ نصوص تحليل الأخطاء ما تزال بالعربية».

### 3.7 التغذية الراجعة والتأكيد — **8/10**

- `restore` يسأل قبل التنفيذ، ويقبل «ن» و«نعم» إلى جانب `y`/`yes` — لمسة صحيحة لأداة عربية:
  ```python
  if answer.strip().lower() not in ("y", "yes", "ن", "نعم"):
  ```
- `--yes` للأتمتة، و`--list` لاستعراض النسخ قبل الاختيار.
- `--dry-run` مطبَّق في **كل** مسار إصلاح بلا استثناء (تحققت: كل من `_fix_timeout_flow`، `_retry_transient_flow`، `_check_rollback`، `_check_circuit_breaker`، `_check_reschedule` يحمل فرع `dry_run`)، وكذلك المصنّف الذكي لا يُستدعى فيه، والمقاييس لا تُكتب.
- `doctor` يعطي فحصاً ذاتياً قبل الاعتماد على الأداة — 7 فحوص مع تمييز الجوهري عن التحذيري.

**الخصم:** لا توجد أي مؤشرات تقدّم. `monitor` على 200 مهمة يستدعي `openclaw` مرة لكل عملية بمهلة 30 ثانية للواحدة، فقد يصمت دقائق بلا سطر واحد. سطر `INFO` لكل مهمة قيد المعالجة يكفي.

### 📊 متوسط قسم UX

| النقطة | الدرجة |
|---|:---:|
| وضوح مخرجات الطرفية | 7 |
| جودة رسائل الخطأ | 8 |
| جودة `--help` | 9 |
| العربية وRTL (HTML) | 8 |
| إمكانية الوصول | 8 |
| i18n | 6 |
| التغذية الراجعة والتأكيد | 8 |

**متوسط القسم: 7.8/10**

---

## 4. المراجعة الأمنية (25%)

**⚠️ تحديد النطاق:** المشروع أداة سطر أوامر محلية بلا خادم وبلا واجهة ويب تفاعلية وبلا مستخدمين متعددين. البنود التالية **لا تنطبق** ولم تُحتسب في الدرجة: المصادقة والجلسات، صلاحيات المسارات، CORS، ترويسات CSP/HSTS/X-Frame-Options، تجزئة كلمات المرور. حُسبت الدرجة من البنود المنطبقة فقط.

### 4.1 المفاتيح والأسرار — **10/10** ✅

- فحص المستودع كاملاً (`.py`, `.json`, `.yml`, `.md`) بنمط أسرار: **صفر نتائج**.
- لا ملف `.env`، ولا أسرار في تاريخ git (12 commit).
- `ANTHROPIC_API_KEY` يُقرأ من البيئة حصراً، وليس مفتاح إعدادات، والقاعدة موثّقة في ثلاثة مواضع (docstring `llm.py`، README، جدول الإعدادات).
- الأذونات مضبوطة بانضباط: `DIR_MODE = 0o700` و`FILE_MODE = 0o600`، مطبَّقة على مجلد العمل والنسخ الاحتياطية وملف الحالة وقاعدة البيانات والقفل وملف السجل — وأي فشل chmod (ويندوز) يُبتلع بهدوء عبر `_chmod_quiet`.

### 4.2 الحقن (Injection) — **9/10** ✅

**تنفيذ الأوامر:** كل استدعاءات `subprocess.run` تمرّر **قائمة وسائط** بلا `shell=True`، ومع `timeout`:

```python
subprocess.run(["openclaw", *args], capture_output=True, text=True, timeout=timeout)
```

اسم مهمة خبيث مثل `"; rm -rf ~"` يصل كوسيط نصي واحد ولا يُفسَّر. لا يوجد مسار حقن أوامر.

**SQL:** كل استعلام مُعامَل (parameterized) بلا استثناء — فحصت `storage.py` سطراً سطراً:

```python
self._conn.execute("SELECT payload FROM llm_cache WHERE hash = ? AND created_at >= ?", (key, cutoff))
```

الاستعلام الوحيد الذي يُبنى ديناميكياً (`_rows`, `daily_success_rates`) يبني **بنية** الجملة فقط (`AND job_id = ?`) والقيم تبقى في `params`.

**HTML:** كل قيمة تمرّ على `_e()` (`html.escape(..., quote=True)`) — فحصت كل استبدال في `render_html` و`_jobs_table` و`_history_table` و`_trend_svg` و`_bars_svg`: لا استثناء. القيم غير المهرَّبة الوحيدة هي مخرجات `_pct`/`_secs` وهي أرقام مُنسَّقة.

**Prometheus:** تسميات القياس مهرَّبة صحيحاً (`\\`, `"`, `\n`) في `_escape_label`.

**الخصم الطفيف (🟡):** `backends/crontab.py` — `run_job()` ينفّذ أمر المهمة عبر الصدفة:

```python
result = subprocess.run(["/bin/sh", "-c", command], capture_output=True, text=True, timeout=300)
```

هذا **مقصود وصحيح** — سطر crontab أمر صدفة بطبيعته، ونظام cron نفسه ينفّذه هكذا. لكن المصدر هنا `crontab -l` للمستخدم نفسه، والتنفيذ يجري في **بيئة CronMaster** (متغيرات بيئة، مجلد عمل، `PATH`) لا بيئة cron — فقد يسلك الأمر سلوكاً مختلفاً عند إعادة المحاولة التلقائية عنه عند التشغيل المجدول.

**الحل:** توثيق الفرق في README تحت جدول الواجهات الخلفية، وتمرير `cwd=Path.home()` و`env` مُصفّى ليقترب من بيئة cron.

### 4.3 التحقق من المدخلات — **7/10**

القيم العددية محروسة بدقة — `_clamp` على 15 مفتاحاً مع تحذير وإرجاع للافتراضي، وفحص خاص لـ `LLM_MIN_CONFIDENCE` في المدى `[0,1]`، وفحص نوع لـ `NOTIFIERS` و`QUIET_HOURS`. وردود المصنّف الذكي محروسة **ثلاث مرات**: `json_schema` مقيَّد بـ `additionalProperties: False` عند الطلب، ثم `data.get("error_type") not in _VALID_TYPES` عند الاستقبال، ثم عتبة `confidence` قبل السماح بأي مسار إصلاح.

**🟠 الفجوة:** المفاتيح النصية بلا فحص نوع — راجع 2.4-ب بالتفصيل. من زاوية أمنية: ملف إعدادات تالف أو مُعدَّل يُسقط الأداة بـ traceback بدل أن يُصحَّح بهدوء، ما يحوّل خطأ إدخال بسيط إلى **توقف صامت للمراقبة** — وأداة مراقبة متوقفة أسوأ من أداة مراقبة تشتكي.

**🟡 ملاحظة ثانوية:** `cli._run_restore` يقبل مساراً مطلقاً خارج مجلد النسخ الاحتياطية:

```python
chosen = Path(args.backup) if args.backup else backups[0]
if not chosen.is_absolute() and not chosen.exists():
    chosen = Config.BACKUP_DIR / chosen.name
```

المسار المطلق يمرّ كما هو، فيُقرأ أي ملف JSON على النظام ويُستعاد منه. الخطورة منخفضة (المستخدم يملك صلاحياته أصلاً، والملف يُتحقق منه في `read_backup`) لكن مبدأ أقل دهشة يقتضي حصر المصدر.

**الحل:** التحقق أن `chosen.resolve()` تقع تحت `Config.BACKUP_DIR.resolve()` وإلا رفض صريح مع تلميح لـ `--list`.

### 4.4 البيانات الحساسة — **7/10**

**🟠 (أ) نص الخطأ يُرسل إلى Anthropic بلا تنقية.**

`analysis/llm.py` — في `_ask()`:

```python
user_text = (
    "صنّف الخطأ التالي لمهمة مجدولة.\n"
    f"اسم المهمة: {job.name}\n"
    ...
    f"{error_text[:4000]}\n"
)
```

القطع عند 4000 محرف هو الحد الوحيد — لا تنقية. ونصوص أخطاء المهام المجدولة تحوي بانتظام: عناوين فيها رموز (`https://api.x/v1?token=...`)، ترويسات `Authorization` في تتبّع HTTP، مسارات فيها أسماء مستخدمين، سلاسل اتصال قواعد بيانات، عناوين IP داخلية.

**إنصافاً للمشروع:** الميزة **معطّلة افتراضياً** (`LLM_ENABLED = False`)، وتحتاج تفعيلاً صريحاً + تثبيت حزمة + مفتاح، ولا تُستدعى إلا حين يعجز المصنّف النمطي (`UNKNOWN`)، ولا تعمل مع `--dry-run`. القرار واعٍ وموثّق. **لكن** README يعدد «الضمانات» ولا يذكر ضمن قائمتها أن محتوى الخطأ يغادر الجهاز بلا تنقية — والمستخدم يستحق أن يقرأ هذا قبل أن يكتب `"llm_enabled": true`.

**الحل:**
1. دالة `redact(text)` في `llm.py` تُطبَّق قبل الإرسال: استبدال أنماط `(?i)(token|api[_-]?key|password|secret|authorization)\s*[=:]\s*\S+` وسلاسل base64 الطويلة و`https?://[^\s]*[?&](token|key)=[^\s&]*` بـ `<redacted>`. المصنّف يحتاج **شكل** الخطأ لا قيمة السر.
2. سطر صريح في قائمة ضمانات README: «نص الخطأ نفسه يُرسل إلى Anthropic — نقّه أو أبقِ الميزة معطّلة إن كانت مهامك تطبع أسراراً في أخطائها».

**🟡 (ب) `http://` مقبول لقنوات Webhook.**

`notifiers/webhook.py`:
```python
def validate(self) -> bool:
    return self.url.startswith(("http://", "https://"))
```

نص التنبيه (أسماء المهام، أوصاف الأخطاء، تفاصيل الإصلاح) يُرسل عندئذ بلا تشفير.

**الحل:** قبول `https://` فقط افتراضياً، وتحذير صريح `"قناة %s تستعمل http بلا تشفير"` عند `http://`، مع إعداد `allow_insecure_webhooks: false` لمن يحتاج شبكة داخلية.

**🟡 (ج) نافذة أذونات قصيرة عند الكتابة.**

`config.write_secure` و`storage.save` تكتبان الملف ثم تضبطان الأذونات:

```python
path.write_text(text, encoding="utf-8")
_chmod_quiet(path, FILE_MODE)
```

بين السطرين يوجد الملف بأذونات `umask` الافتراضية (`0644` غالباً). الخطورة **محدودة جداً** لأن المجلد الأب `0700` فلا وصول أصلاً، لكن الحل نظيف: `os.open(path, O_WRONLY|O_CREAT|O_TRUNC, 0o600)` يخلق الملف بالأذونات الصحيحة ذرّياً.

### 4.5 الاعتماديات — **10/10** ✅

```toml
dependencies = []
```

صفر اعتماديات تشغيل — أقوى وضع أمني ممكن: لا سلسلة توريد، لا `npm audit`، لا CVE موروثة. الاعتمادية الاختيارية الوحيدة (`anthropic>=0.40`) خلف استيراد كسول داخل `try/except ImportError`، وغيابها لا يكسر شيئاً. وCI يحرس هذا صراحةً باختبار دخان يشغّل الأداة بلا تثبيت أي شيء:

```yaml
- name: Stdlib-only smoke test
  run: python3 CronMaster_AI.py --version
```

حراسة معمارية مكتوبة في CI — ممتاز.

### 4.6 التزامن وذرّية الكتابة — **8/10**

- كتابة `state.json` ذرّية (`tmp` ثم `os.replace`) — انقطاع منتصف الكتابة لا يترك ملفاً تالفاً.
- كتابة ملف Prometheus ذرّية للسبب نفسه (`node_exporter` لن يقرأ ملفاً نصفياً).
- قفل التنفيذ يستعمل `fcntl.flock` على POSIX — يتحرر تلقائياً بموت العملية — مع سقوط إلى pidfile يستعيد الأقفال المهجورة عبر `os.kill(pid, 0)`.
- ملف الحالة التالف يُقرأ، يُحذَّر منه، وتبدأ حالة جديدة بدل السقوط.

**🟡 الثغرة:** `backends/crontab.py` — `_rewrite()` تقرأ crontab كاملاً، تعدّل سطراً، وتكتب الكل مرة أخرى (`crontab -`) بلا أي قفل:

```python
lines = self.read_raw().splitlines()
... # تعديل
result = run_crontab("-", stdin=content)
```

إن عدّل المستخدم crontab (`crontab -e`) بين القراءة والكتابة، تُفقد تعديلاته صامتة. القفل الداخلي `ExecutionLock` يحمي من تشابك دورتَي CronMaster، لا من مستخدم بشري.

**الحل:** حفظ بصمة `sha256` للمحتوى المقروء، وإعادة قراءة crontab قبل الكتابة والتحقق من تطابق البصمة — وإلا إلغاء العملية مع رسالة «تغيّر crontab أثناء التعديل — أُلغي التغيير، أعد المحاولة».

### 📊 متوسط القسم الأمني

| البند | الدرجة | الحالة |
|---|:---:|---|
| المفاتيح والأسرار | 10 | ✅ نظيف |
| الحقن (أوامر / SQL / HTML) | 9 | ✅ مؤمَّن |
| التحقق من المدخلات | 7 | 🟠 فجوة نصية |
| البيانات الحساسة | 7 | 🟠 تنقية ناقصة |
| الاعتماديات | 10 | ✅ صفر اعتماديات |
| التزامن وذرّية الكتابة | 8 | 🟡 crontab بلا قفل |
| المصادقة والجلسات | — | لا ينطبق (بلا خادم) |
| CORS والترويسات | — | لا ينطبق (بلا خادم) |
| تجزئة كلمات المرور | — | لا ينطبق (بلا مستخدمين) |

**متوسط القسم (من المنطبق فقط): 8.6/10**

---

## 5. الدرجة الإجمالية المرجّحة

| الناحية | الدرجة | الوزن | المساهمة |
|---|:---:|:---:|:---:|
| تقنية وهندسية | 8.2/10 | 45% | 3.69 |
| UX/UI (معيار CLI + تقرير HTML) | 7.8/10 | 30% | 2.34 |
| أمن سيبراني | 8.6/10 | 25% | 2.15 |
| **الإجمالي** | | | **8.2/10** |

**الحساب:** (8.2 × 0.45) + (7.8 × 0.30) + (8.6 × 0.25) = 3.69 + 2.34 + 2.15 = **8.18 ≈ 8.2/10**

### الحكم بصراحة

هذا مشروع **فوق المتوسط بوضوح**، وأقوى ما فيه أنه بُني بانضباط هندسي حقيقي لا بمظهره: عقد القدرات بين المنسّق والواجهات الخلفية، التدهور اللطيف في كل مسار فشل، صفر اعتماديات محروسة باختبار دخان في CI، 272 اختباراً معزولاً بتغطية 87%، وتعليقات تحفظ **سبب** القرار لا وصفه. الأمن نظيف فعلاً — لا حقن، لا أسرار، أذونات مضبوطة — ولم أضطر لاختراع ثغرات لا محل لها.

ما يمنعه من التسعة ثلاثة أشياء ملموسة: **ثغرة تكرار التنبيه** التي تناقض وعداً صريحاً في README وتفسد الوظيفة الأساسية للأداة عند التشغيل المتكرر، و**فجوة الأداء** في مسار الإحصائيات (101 استعلام لعشرين مهمة، و20 fsync لدورة واحدة)، و**i18n نصف منجَز** يُسوَّق كميزة مكتملة بينما جوهر ما يقرأه المستخدم — تشخيص أخطائه — يبقى عربياً في الواجهة الإنجليزية.

الثلاثة قابلة للإصلاح بأيام عمل معدودة على بنية لا تحتاج أي إعادة كتابة. المشروع جاهز للاستخدام الفعلي **بعد** إصلاح البند الأول تحديداً.

### ما لا يمكن تقييمه من الكود وحده

- **السلوك الفعلي مع OpenClaw CLI:** غير متاح في بيئة المراجعة. صحة أسماء الأعلام (`--timeout-seconds`, `--cron`) وشكل مخرجات `cron list --json` لم تُتحقق ميدانياً — اعتُمد على الكود والتوثيق.
- **جودة تصنيف المصنّف الذكي:** تحتاج مجموعة أخطاء حقيقية موسومة لقياس الدقة والاستدعاء. البنية والحواجز مراجَعة، أما جودة الأحكام فلا.
- **الأداء تحت حمل حقيقي:** الأرقام المذكورة (101 استعلام، 20 commit) مقيسة معملياً على بيانات مُصنَّعة. الأثر الفعلي يعتمد على عدد المهام ومدة الاحتفاظ ونوع القرص.
- **تجربة التقرير على أجهزة فعلية:** فُحص كود HTML/CSS لا عرضه في متصفحات وقارئات شاشة حقيقية.

---

## 6. خطة الإصلاح المرتبة بالأولوية

| # | الأولوية | الناحية | المشكلة | الحل المقترح | التأثير المتوقع |
|:-:|:-:|---|---|---|---|
| 1 | 🔴 حرج | هندسة | `core.py` — `_fix_timeout_flow()`: تصعيد الإصلاح غير المجدي يستدعي `_escalate` بلا فحص تهدئة، فيُرسَل تنبيه متطابق كل دورة (تحقق: 5 تنبيهات / 5 دورات) — مخالف لوعد `alert_cooldown_hours` في README | تغليف `self._escalate(...)` بـ `if dry_run or self.state.should_alert(job.id, "futile_fix")` مع `record_alert(job.id, "futile_fix")` بعده. `clear_alerts_for` عند التعافي يعيد التنبيه تلقائياً | تنبيه واحد كل 24 ساعة بدل 288؛ التنبيهات تستعيد معناها ولا يكتمها المستخدم |
| 2 | 🔴 حرج | هندسة | لا اختبار يشغّل `monitor()` أكثر من دورة واحدة — ولهذا مرّت الثغرة #1 بلا كشف رغم تغطية 87% | `tests/test_multi_cycle.py`: تشغيل 3–5 دورات وإثبات خصائص عبر الزمن (التنبيه لا يتكرر، العدّادات لا تتجاوز السقف، `state.json` لا ينمو، لا تكرار صفوف `runs`) | كشف صنف كامل من العيوب الزمنية غير المرئي حالياً؛ حماية دائمة للإصلاح #1 |
| 3 | 🟠 مهم | أمن + هندسة | `config.py` — `validate()`: لا تفحص نوع `_STR_KEYS`. `{"lang": 5}` أو `{"backend": 7}` → `AttributeError` وtraceback خام؛ و`{"telegram_chat_id": 123456}` (الخطأ الأرجح) يوقف التنبيهات بصمت | حلقة في `validate()` تُحوّل كل مفتاح في `_STR_KEYS` إلى `str` مع تحذير؛ + فحص أنواع في `doctor` | ملف إعدادات خاطئ يُصحَّح بهدوء بدل إسقاط المراقبة؛ اتساق مع فلسفة `_clamp` القائمة |
| 4 | 🟠 مهم | أمن | `analysis/llm.py` — `_ask()`: نص الخطأ يُرسل إلى Anthropic بلا تنقية (القطع عند 4000 محرف هو الحد الوحيد)، وقد يحوي رموزاً وسلاسل اتصال | دالة `redact(text)` تُطبَّق قبل البناء (أنماط token/key/password/authorization، base64 طويل، معاملات URL)؛ + سطر صريح في قائمة ضمانات README | إغلاق قناة تسريب أسرار إلى طرف ثالث بلا فقدان دقة التصنيف (المصنّف يحتاج شكل الخطأ لا قيمة السر) |
| 5 | 🟠 مهم | أداء | `storage.py` — `job_summary()`: خمسة استعلامات مستقلة لكل مهمة (قياس: 101 SELECT لعشرين مهمة)، و`mean_time_between_failures` تسحب كل الصفوف بلا `LIMIT` | استبدالها باستعلام تجميعي واحد `GROUP BY job_id` يحسب `COUNT`/`SUM(status='ok')`/`AVG(duration)` في SQL؛ + نافذة `LIMIT` على mtbf. الفهرس `idx_runs_job_obs` موجود | `stats` وتقرير HTML يتحولان من ~5N استعلاماً إلى 1–2؛ استهلاك ذاكرة ثابت بدل خطّي مع مدة الاحتفاظ |
| 6 | 🟠 مهم | أداء | `storage.py` — `record_observation()` تُنهي كل إدراج بـ `commit()` فتصير `record_cycle()` عشرين fsync (قياس: 20 COMMIT / 20 مهمة) | `executemany` داخل معاملة واحدة و`commit` واحد في نهاية `record_cycle` | fsync واحد بدل N؛ دورة المراقبة أسرع ملموساً على أقراص بطيئة |
| 7 | 🟠 مهم | UX | i18n نصف منجَز: `errors_db.py` (9 توقيعات) ورسائل `fixers.py` و`core.py` عربية داخل الكود، فـ `--lang en` يعطي واجهة إنجليزية وتحليلاً عربياً | نقل `description_ar`/`suggested_fix` إلى مفاتيح i18n وجعل `ErrorSignature` تحمل `key`؛ تمرير رسائل الإصلاح عبر `t()`. وحتى يُنجَز: تصحيح وصف الميزة في README | `--lang en` يصير ميزة حقيقية لا جزئية؛ زوال التناقض بين الوعد والسلوك |
| 8 | 🟡 تحسين | UX | `cli.py` — `_print_status()` يستعمل مسافات ثابتة مُعايَرة للنصوص العربية، فالمحاذاة تتعرّج بالإنجليزية (تحقق: انظر المخرَج في 3.6) | حشو محسوب: `f"{label:<{width}}{value}"` مع `width = max(len(l) for l in labels)` | محاذاة صحيحة في اللغتين؛ إلغاء التعليق الاعتذاري في الكود |
| 9 | 🟡 تحسين | UX | `monitor` و`list` و`fix` و`history` تسكب JSON خاماً؛ مخرجات `monitor` عشرات الأسطر بلا ملخص بشري | علم `--json`؛ الافتراضي عرض بشري (ملخص + جدول المهام الفاشلة)، مع إبقاء JSON عند `not sys.stdout.isatty()` حتى لا تنكسر السكربتات | أمر `monitor` اليدوي يصير مقروءاً؛ لا كسر للتوافق الآلي |
| 10 | 🟡 تحسين | هندسة | `cli.main()` يلتقط `BackendError` وحده؛ أي `OSError`/`sqlite3.Error` غير متوقع يخرج traceback خام | `except Exception` أخير: تسجيل كامل في `cronmaster.log` + سطر عربي واحد للمستخدم يشير إلى ملف السجل + `EXIT_MONITOR_FAILURE` | اتساق تجربة الخطأ مع بقية الأداة الممتازة في هذا الجانب |
| 11 | 🟡 تحسين | أمن | `notifiers/webhook.py` — `validate()` تقبل `http://`، فنص التنبيه يُرسل بلا تشفير | قبول `https://` فقط افتراضياً + تحذير صريح عند `http://` + إعداد `allow_insecure_webhooks` للشبكات الداخلية | إغلاق تسريب محتوى التنبيهات على الشبكة بلا كسر حالات الاستخدام المشروعة |
| 12 | 🟡 تحسين | أمن | `backends/crontab.py` — `_rewrite()` قراءة‑تعديل‑كتابة بلا قفل: تعديل بشري متزامن (`crontab -e`) يُفقد صامتاً | بصمة `sha256` للمحتوى المقروء، وإعادة قراءة والتحقق قبل الكتابة، وإلغاء مع رسالة واضحة عند الاختلاف | لا فقدان صامت لجدولة المستخدم |
| 13 | 🟡 تحسين | أمن | `config.write_secure` و`storage.save` تكتبان ثم تضبطان الأذونات — نافذة قصيرة بأذونات `umask` | `os.open(path, O_WRONLY\|O_CREAT\|O_TRUNC, 0o600)` لخلق الملف بأذوناته الصحيحة ذرّياً | إغلاق نافذة TOCTOU (محدودة الأثر أصلاً بمجلد `0700`) |
| 14 | 🟡 تحسين | هندسة | تكرار `_fmt_pct`/`_fmt_secs` في `cli.py` مقابل `_pct`/`_secs` في `reporting/html.py` — نفس المنطق حرفياً في ملفين | وحدة `cronmaster/format.py` مشتركة (`pct`, `secs`, `duration`, `trend`) ويستوردها الاثنان | مصدر واحد لصيغة العرض؛ لا انحراف صامت بين الطرفية والتقرير |
| 15 | 🟡 تحسين | UX | تقرير HTML: `white-space: nowrap` مع `min-width: 40rem` يفرض تمريراً أفقياً لكل جدول على الجوال | إزالة `nowrap` عن عمودَي «المهمة» و«التفاصيل»؛ أو `@media (max-width: 40rem)` يحوّل الصفوف إلى بطاقات | جداول التقرير تصير قابلة للقراءة على الجوال |
| 16 | 🟡 تحسين | UX | جداول HTML بلا `<caption>` وبلا `scope="col"` — قارئ الشاشة لا يربط الخلايا برؤوسها | `<th scope="col">` في `_jobs_table` و`_history_table` + `<caption>` بنص من i18n | إمكانية وصول أفضل بتعديل سطرين |
| 17 | 🟡 تحسين | هندسة | `PROJECT_REVIEW.md` يصف بنية ما قبل الحزمة ويذكر `run_status.py` و`run_test.py` غير الموجودَين | نقله إلى `docs/archive/PROJECT_REVIEW-2026-07.md` مع سطر رأس: «مراجعة تاريخية — لا تصف الكود الحالي» | لا يضلَّل قارئ جديد بوثيقة منتهية الصلاحية |
| 18 | 🟡 تحسين | هندسة | `requires-python = ">=3.8"` بينما CI يختبر 3.9 و3.12 فقط، و`zoneinfo` (فترة الهدوء بالمنطقة الزمنية) تحتاج 3.9+ | رفع `requires-python` إلى `">=3.9"` وحذف مسار السقوط الميت في `notifiers/__init__.py` — أو إضافة 3.8 إلى مصفوفة CI | زوال وعد دعم غير مُختبَر؛ حذف كود ميت |
| 19 | 🟡 تحسين | هندسة | `storage.py` — `record_fix()` تنادي `save()` فتُكتب الحالة كاملة عند كل إصلاح، بالإضافة إلى الحفظ النهائي في `monitor()` | إزالة `self.save()` من `record_fix` (المسار الوحيد المتأثر `core.fix_job()` ينادي `save()` صراحةً بعده أصلاً) | كتابة واحدة للحالة لكل دورة بدل N+1 |
| 20 | 🟡 تحسين | هندسة | `core.CronMaster` منسّق ضخم (641 سطراً)، و`monitor()` وحدها ~130 سطراً بست مسؤوليات | استخراج مسارات الإصلاح الخمسة إلى `cronmaster/policies.py` تعرض `apply_policies(job, analysis, ctx) -> PolicyOutcome`؛ `monitor()` تصير حلقة تنسيق قصيرة | اختبار كل سياسة إصلاح على حدة؛ إضافة سياسة جديدة بلا لمس المنسّق |
| 21 | 🟡 تحسين | هندسة | خلط أسلوبَي التسجيل: `logger.info(f"...")` و`logger.info("%s", ...)` في الملف الواحد | توحيد على النمط الكسول + تفعيل قاعدة `G` في `[tool.ruff.lint] select` لفرضه آلياً | اتساق + تجنّب بناء سلاسل لا تُسجَّل |
| 22 | 🟡 تحسين | UX | لا مؤشر تقدّم: `monitor` على مئات المهام قد يصمت دقائق (مهلة 30 ثانية لكل استدعاء `openclaw`) | سطر `INFO` لكل مهمة قيد المعالجة، وعدّاد `[i/N]` في المخرجات البشرية | المستخدم يفرّق بين «تعمل» و«معلّقة» |
| 23 | 🟡 تحسين | أمن | `cli._run_restore` يقبل مساراً مطلقاً خارج `Config.BACKUP_DIR` | التحقق أن `chosen.resolve()` تحت `Config.BACKUP_DIR.resolve()` وإلا رفض صريح مع تلميح `--list` | مبدأ أقل دهشة؛ حصر مصدر الاستعادة |
| 24 | 🟡 تحسين | هندسة | `Config` حالة عامة قابلة للتعديل — يُجبر `conftest.py` على سرد 19 خاصية يدوياً لعزل الاختبارات | تحويل `Config` إلى `@dataclass` غير قابل للتعديل يُبنى في `main()` ويُمرَّر إلى `CronMaster(config=...)` | عزل الاختبار يصير `Config(**overrides)`؛ لا تسرّب حالة عند إضافة إعداد جديد |

---

**منهج المراجعة:** قُرئ كل ملف مصدر بالكامل (لا عيّنة). شُغِّلت مجموعة الاختبارات و`ruff` وقياس التغطية. الملاحظات 1 و3 و5 و6 والمخرجات في 3.6 **مُتحقَّق منها عملياً** باختبارات فحص مؤقتة كُتبت لهذا الغرض ثم حُذفت — الأرقام المذكورة مقيسة لا مقدَّرة.
